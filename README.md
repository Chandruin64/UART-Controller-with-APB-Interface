# UART-Controller-with-APB-Interface
Designed and verified a configurable UART controller with an APB-based programming interface to enable processor-controlled asynchronous serial communication. The project combined RTL development with a complete UVM-based verification environment to validate functionality across configuration modes and protocol corner cases.
